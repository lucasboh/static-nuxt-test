{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev --port 3333",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@nuxtjs/strapi": "^1.6.3",
    "fibers": "^5.0.3",
    "nuxt": "3.0.0",
    "sass": "^1.57.1",
    "sass-loader": "10"
  },
  "dependencies": {
    "normalize.css": "^8.0.1"
  }
}
